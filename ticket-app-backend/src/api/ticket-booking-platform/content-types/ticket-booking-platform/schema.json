{
  "kind": "collectionType",
  "collectionName": "ticket_booking_platforms",
  "info": {
    "singularName": "ticket-booking-platform",
    "pluralName": "ticket-booking-platforms",
    "displayName": "Ticket-booking-platform"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "Passengers": {
      "type": "enumeration",
      "enum": [
        "Only 1",
        "Between 2-5",
        "Above 5"
      ],
      "default": "Only 1",
      "required": true
    },
    "Name": {
      "type": "string",
      "required": true
    },
    "Class": {
      "type": "enumeration",
      "enum": [
        "Economy",
        "Business ",
        "First class"
      ],
      "default": "Economy",
      "required": true
    }
  }
}
